# Port the HTTP server listens on.
# Optional — defaults to 8080 if unset.
PORT=8080

# Absolute path to the SQLite usage database.
# Optional — defaults to <project-root>/data/usage.db if unset.
# USAGE_DB_PATH=/custom/path/usage.db
